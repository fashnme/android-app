import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
    addToCartButton: {
        padding: 10,
        borderWidth: 1,
        borderRadius: 25,
        justifyContent: 'center',
        alignItems: 'center',
        margin: 10,
    },
    addToCartButtonText: {
        color: 'black',
    },
    buyNowButton: {
        padding: 10,
        backgroundColor: 'red',
        color: 'white',
        borderRadius: 25,
        justifyContent: 'center',
        alignItems: 'center',
    },
    buyNowButtonText: {
        color: 'white',
    }
});
